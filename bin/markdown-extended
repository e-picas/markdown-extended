#!/usr/bin/env php
<?php
/*
 * This file is part of the PHP-MarkdownExtended package.
 *
 * (c) Pierre Cassat <me@e-piwi.fr> and contributors
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */

// show errors at least initially
@ini_set('display_errors','1'); @error_reporting(E_ALL & ~E_NOTICE & ~E_STRICT);

// namespaces loader
if (file_exists($bt = __DIR__.'/../src/bootstrap.php')) {
    require_once $bt;
} else {
    echo 'MarkdownExtended bootstrapper not found!';
    exit(1);
}

// silent errors
@error_reporting(-1);

// launch console API
if (strpos(php_sapi_name(),'cli')!==false) {
    @set_time_limit(0);
    \MarkdownExtended\MarkdownExtended::getInstance()
        ->get('CommandLine\Console')
        ->run();
} else {
    echo 'This file is for command line usage only!';
    exit(1);
}

// Endfile
